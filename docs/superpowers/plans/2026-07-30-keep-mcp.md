# Keep MCP Implementation Plan

> **For agentic workers:** Subagent-driven or inline execution. Checkboxes track progress.

**Goal:** Ship a separate Keep MCP control plane (`ravenstack-keep`) with approved tools (all sections except E), provisional SOT seeds, and research prompts for SOT + surface organization.

**Architecture:** Two planes — `reclaw-platform` (ops) vs `ravenstack-keep` (rooms/specs/scope/gates). FastMCP + SQLite + Agent Spec files. Gated tools require `confirm=true`.

**Tech Stack:** Python 3.12+, mcp==1.12.4 FastMCP, SQLite, jsonschema, httpx, pytest

## Global Constraints

- No draft-to-execute  
- No section E cost pipeline yet  
- No second OpenClaw gateway on laptop  
- Map SOT provisional until Claude memo  
- Tailscale-first for Keep; no public tunnel v0  

---

### Task 1: Package scaffold + seeds

**Files:**
- Create: `mcp/src/ravenstack_keep_mcp/*`, `mcp/seeds/castle_map.provisional.json`, `mcp/requirements.txt`

- [x] Layout + provisional castle map  
- [x] paths/store/specs/knowledge modules  

### Task 2: Tool surface

**Files:**
- Create: `mcp/src/ravenstack_keep_mcp/server.py`

- [x] A core tools  
- [x] B map/A2A tools  
- [x] C governance gated tools  
- [x] D rituals/hygiene  
- [x] F stubs + H meta  
- [x] E deferred (not implemented)  

### Task 3: Docs + research prompts

**Files:**
- Create: `docs/ARCHITECTURE-MCP-SPLIT.md`, `prompts/PROMPT-SOT-RESEARCH-CLAUDE.md`, `prompts/PROMPT-MCP-SURFACE-ORGANIZATION-GEMINI.md`, `mcp/tools.md`, `mcp/README.md`

- [x] Architecture split  
- [x] Claude SOT prompt  
- [x] Gemini organization prompt  
- [x] Tool catalog + README  

### Task 4: Tests

**Files:**
- Create: `mcp/tests/test_keep_mcp.py`

- [x] pytest green  

### Task 5: Optional local Grok wiring

- [ ] Add `[mcp_servers.ravenstack-keep]` to `~/.grok/config.toml` when user wants  
- [ ] Hetzner systemd deploy — **not** this laptop session unless asked  

---

## Verify

```bash
cd ~/ravenstack-keep
PYTHONPATH=mcp/src .venv/bin/pytest mcp/tests -v
```
