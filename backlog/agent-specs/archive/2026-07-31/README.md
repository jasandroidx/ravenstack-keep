# Archived Agent Spec drafts — 2026-07-31

Operator chose to keep these as archive (not approve/live yet):

| id | purpose (short) |
|----|-----------------|
| package-critic | Quality veto Accept/Revise/Kill before human gate |
| skill-forger | Draft skills from wins/corrections; never auto-install |
| model-budget-warden | Report free/balanced/heavy; warn paid-first defaults |

Restore: move `*.agent-spec.draft.json` back to `backlog/agent-specs/` then `approve_spec(confirm=true)`.
